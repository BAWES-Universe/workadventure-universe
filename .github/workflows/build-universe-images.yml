name: Build Universe Images for Coolify

on:
  push:
    branches: [universe]
    paths:
      - 'play/**'
      - 'back/**'
      - 'map-storage/**'
      - 'uploader/**'
      - 'discord-bot/**'
      - 'bots/**'
      - 'libs/**'
      - 'messages/**'
      - 'contrib/docker/docker-compose.universe.yaml'
      - 'scripts/*universe*'
      - '.github/workflows/build-universe-images.yml'
  workflow_dispatch:
    inputs:
      service:
        description: 'Service to build (play, back, map-storage, uploader, discord-bot, bot-server, or all)'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - play
          - back
          - map-storage
          - uploader
          - discord-bot
          - bot-server

env:
  REGISTRY: ghcr.io
  # Change this to your GitHub username or organization
  IMAGE_PREFIX: ${{ github.repository_owner }}

jobs:
  build-play:
    if: ${{ github.event.inputs.service == 'all' || github.event.inputs.service == 'play' || github.event.inputs.service == 'bot-server' || github.event_name == 'push' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/play-universe
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./play/Dockerfile.universe
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          build-args: |
            NODE_OPTIONS=--max-old-space-size=16384
            FAST_BUILD=
            SENTRY_RELEASE=
            SENTRY_URL=
            SENTRY_AUTH_TOKEN=
            SENTRY_ORG=
            SENTRY_PROJECT=
            SENTRY_ENVIRONMENT=
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-back:
    if: ${{ github.event.inputs.service == 'all' || github.event.inputs.service == 'back' || github.event_name == 'push' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/back-universe
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./back/Dockerfile.universe
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-map-storage:
    if: ${{ github.event.inputs.service == 'all' || github.event.inputs.service == 'map-storage' || github.event_name == 'push' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/map-storage-universe
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./map-storage/Dockerfile.universe
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-uploader:
    if: ${{ github.event.inputs.service == 'all' || github.event.inputs.service == 'uploader' || github.event_name == 'push' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/uploader-universe
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./uploader/Dockerfile.universe
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-discord-bot:
    if: ${{ github.event.inputs.service == 'all' || github.event.inputs.service == 'discord-bot' || github.event_name == 'push' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/discord-bot-universe
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./discord-bot
          file: ./discord-bot/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-bot-server:
    needs: build-play
    if: ${{ github.event.inputs.service == 'all' || github.event.inputs.service == 'bot-server' || github.event_name == 'push' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/bot-server-universe
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./bots/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max


  # ─────────────────────────────────────────────────
  # Release: auto-version, tag, and GitHub Release
  # Runs only on push to universe (not PRs)
  # ─────────────────────────────────────────────────
  release:
    if: github.ref == 'refs/heads/universe' && github.event_name == 'push'
    needs: [build-play, build-back, build-map-storage, build-uploader, build-discord-bot, build-bot-server]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      new_version: ${{ steps.calculate.outputs.new_version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Calculate next version
        id: calculate
        run: |
          PKG_VERSION=$(cat VERSION)
          LATEST_TAG=$(git tag -l 'v[0-9]*.[0-9]*.[0-9]*' --sort=-v:refname | head -1)
          LATEST_VERSION="${LATEST_TAG#v}"

          echo "VERSION file: $PKG_VERSION"
          echo "Latest tag:   $LATEST_TAG"

          if [ -z "$LATEST_TAG" ]; then
            NEW_VERSION="$PKG_VERSION"
          elif [ "$PKG_VERSION" = "$LATEST_VERSION" ]; then
            IFS='.' read -r MAJOR MINOR PATCH <<< "$PKG_VERSION"
            NEW_VERSION="${MAJOR}.${MINOR}.$((PATCH + 1))"
          elif [ "$(printf '%s\n' "$PKG_VERSION" "$LATEST_VERSION" | sort -V | tail -1)" = "$PKG_VERSION" ]; then
            # VERSION is ahead of latest tag — use VERSION (user bumped it)
            NEW_VERSION="$PKG_VERSION"
          else
            # VERSION is behind latest tag — bump from tag instead
            IFS='.' read -r MAJOR MINOR PATCH <<< "$LATEST_VERSION"
            NEW_VERSION="${MAJOR}.${MINOR}.$((PATCH + 1))"
          fi

          echo "new_version=$NEW_VERSION" >> "$GITHUB_OUTPUT"

      - name: Create tag and release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NEW_VERSION: ${{ steps.calculate.outputs.new_version }}
        run: |
          VERSION="v${NEW_VERSION}"

          if git rev-parse "$VERSION" >/dev/null 2>&1; then
            echo "Tag $VERSION already exists — skipping"
            exit 0
          fi

          git config user.name "BAWES Bot"
          git config user.email "bot@bawes.net"
          git tag "$VERSION"
          git push origin "$VERSION"

          gh release create "$VERSION" --generate-notes

  # ─────────────────────────────────────────────────
  # Deploy: trigger Coolify to pull new images
  # Fires only if release produced a new version
  # ─────────────────────────────────────────────────
  deploy:
    if: needs.release.outputs.new_version != ''
    needs: release
    runs-on: ubuntu-latest
    steps:
      - name: Deploy all universe services to Coolify
        env:
          COOLIFY_BASE: ${{ secrets.COOLIFY_BASE }}
          COOLIFY_TOKEN: ${{ secrets.COOLIFY_TOKEN }}
          COOLIFY_PLAY_UUID: ${{ secrets.COOLIFY_PLAY_UUID }}
          COOLIFY_BACK_UUID: ${{ secrets.COOLIFY_BACK_UUID }}
          COOLIFY_MAP_STORAGE_UUID: ${{ secrets.COOLIFY_MAP_STORAGE_UUID }}
          COOLIFY_BOT_SERVER_UUID: ${{ secrets.COOLIFY_BOT_SERVER_UUID }}
          COOLIFY_DISCORD_BOT_UUID: ${{ secrets.COOLIFY_DISCORD_BOT_UUID }}
          COOLIFY_UPLOADER_UUID: ${{ secrets.COOLIFY_UPLOADER_UUID }}
        run: |
          echo "Deploying v${{ needs.release.outputs.new_version }}..."
          for UUID in \
            "${COOLIFY_PLAY_UUID}" \
            "${COOLIFY_BACK_UUID}" \
            "${COOLIFY_MAP_STORAGE_UUID}" \
            "${COOLIFY_BOT_SERVER_UUID}" \
            "${COOLIFY_DISCORD_BOT_UUID}" \
            "${COOLIFY_UPLOADER_UUID}"; do
            echo "Deploying $UUID..."
            curl -sSf -X POST "${COOLIFY_BASE}/api/v1/deploy?uuid=${UUID}" \
              -H "Authorization: Bearer ${COOLIFY_TOKEN}" || echo "Failed: $UUID"
          done
          echo "All services deployed."
